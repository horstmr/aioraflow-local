# Modo navegador (run_web.py): não precisa do pywebview.
# Mais leve e sem dependências gráficas nativas — instala igual em Windows, macOS e Linux.
flask>=3.0
anthropic>=0.97
faster-whisper>=1.0
imageio-ffmpeg>=0.5
python-docx>=1.1
