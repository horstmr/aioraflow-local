# Configuração local com a chave da API — não versionar
config.json

# Banco de dados local
data/

# Python
__pycache__/
*.pyc

# Snapshot original baixado (referência)
_raw/

# Artefatos de build e pacotes (o zip vai como release asset, não no Git)
build/
dist/
*.zip
# Obs.: AioraFlow.spec É versionado (usado pelo build de CI); não ignorar.
